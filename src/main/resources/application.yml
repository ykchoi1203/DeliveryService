spring:
  application:
    name: Delivery

  profiles:
    group:
      logging: dev, log
    active: dev

  config:
    import:
      - classpath:/yaml/application-dev.yml

logging:
  level:
    root: INFO
    com.younggeun.delivery: DEBUG
